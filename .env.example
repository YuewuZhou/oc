# .env.example — copy to .env and fill in your values.
#
#   cp .env.example .env
#   # edit .env, then test:
#   echo "say hello" | bash bin/greg.sh

# ── Claude / Chris ───────────────────────────────────────────────────────────
# Used by: Claude Code CLI, Chris subagents (claude -p)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# ── OpenAI / Kevin + Search ──────────────────────────────────────────────────
# Used by: Kevin (openclaude → gpt-5.4-mini), search tool (gpt-4o-search-preview)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# ── Gemini / Greg ────────────────────────────────────────────────────────────
# Used by: Greg subagents (gemini-3.1-flash-lite, gemma models, etc.)
# Get from: https://aistudio.google.com/app/apikey (free tier available)
# Add more keys for round-robin rotation to reduce rate-limit hits:
GEMINI_API_KEY=AIza...
# GEMINI_API_KEY_2=AIza...
# GEMINI_API_KEY_3=AIza...

# ── Email (Jarvan SMTP/IMAP) ─────────────────────────────────────────────────
# Used by: tools/email/send_report.py, poll_once.py
# Gmail app password — NOT your account password.
# Get from: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=your-gmail@gmail.com

IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=your-gmail@gmail.com
IMAP_PASS=your-app-password
IMAP_MAILBOX=INBOX
IMAP_POLL_INTERVAL=30
